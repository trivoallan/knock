# The reference consumer: reads the provenance stamp and computes blast radius.
# On-demand (re-run with `make blast-radius`); ttl-cleaned after it finishes.
apiVersion: batch/v1
kind: Job
metadata:
  name: knock-blast-radius
spec:
  backoffLimit: 1
  ttlSecondsAfterFinished: 600
  template:
    metadata:
      labels:
        app.kubernetes.io/name: knock
        app.kubernetes.io/component: blast-radius
    spec:
      serviceAccountName: knock
      automountServiceAccountToken: true   # the blast-radius consumer calls the kube API (digest -> cluster)
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: blast-radius
          image: knock # rewritten by the kustomize images transformer; has regctl + python3
          command: ["bash", "/scripts/blast-radius.sh"]
          env:
            - name: HOME # regctl persists its registry config under $HOME/.regctl
              value: /tmp
            - name: BLAST_REPOS
              valueFrom:
                configMapKeyRef:
                  name: knock-config
                  key: BLAST_REPOS
          envFrom:
            - secretRef:
                name: knock-registries # same roster knock uses (host + creds)
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: script
              mountPath: /scripts
              readOnly: true
            - name: home
              mountPath: /tmp
      volumes:
        - name: script
          configMap:
            name: knock-blast-radius
            defaultMode: 0755
        - name: home
          emptyDir: {}
