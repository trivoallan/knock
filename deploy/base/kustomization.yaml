# knock reference deployment — runtime-neutral core.
#
# This base is identical across the Argo reference (make demo) and the local
# escape hatch (make local); they change ONLY the registry target, the secret
# source (ESO->OpenBao vs a plain Secret), the image, and whether the CronJob is
# suspended (the buildkitd rebuild add-on is present in both).
# That identity is what guarantees "the demo IS the blueprint" (no drift).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: knock

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - cronjob-reconcile.yaml
  - cronjob-gc.yaml
  - job-blast-radius.yaml
  - rbac-dt-bootstrap.yaml
  - rbac-blast-radius.yaml
  - job-dt-bootstrap.yaml
  - job-publish-sbom.yaml
  - job-scan-attach.yaml
  - job-seed-incident.yaml

# Stable, predictable resource names (no content-hash suffix) so the on-demand
# blast-radius Job and the CronJob reference config/script by fixed name.
generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: knock-config
    literals:
      - KNOCK_LABEL_PREFIX=io.knock
      # Human-readable console logs in the reconcile/blast-radius Jobs — `make logs`
      # and `kubectl logs` are read by a person watching the demo, so text beats JSON
      # here (and matches knock's own default). Flip to json where a log pipeline
      # (Loki/ELK) ingests these structured events instead.
      - KNOCK_LOG_FORMAT=text
      - KNOCK_LOG_LEVEL=INFO
      - KNOCK_WORK_DIR=/tmp/knock-work
      # GitOps source of the policies (a merged PR here is the front door).
      # Overlays repoint these at the org's policy repo.
      - POLICY_REPO_URL=https://github.com/trivoallan/knock
      - POLICY_REPO_REF=main
      - POLICY_DIR=/policies/current/docs/examples/reference
      # Repos the blast-radius consumer walks (the reference policy: copy + rebuild).
      - BLAST_REPOS=demo/busybox demo/debian demo/debian-xz bypassed/debian-xz
      # Number of shards for the Indexed reconcile Job (= completions). Both
      # this value and jobTemplate.spec.completions must be bumped together per
      # overlay; base is 1 (identical to the former non-indexed behaviour).
      - SHARD_COUNT=1
      # SBOM formats syft attaches (one OCI referrer each) on every placed image. CycloneDX is
      # what Dependency-Track ingests (publish-sbom fetches that referrer); SPDX kept as the
      # portable default. Always-on; this only chooses formats. (ADR 0034 / #140)
      - 'KNOCK_SBOM_FORMATS=["spdx-json","cyclonedx-json"]'
  # The blast-radius consumer script, mounted into its Job. Canonical copy
  # lives at scripts/blast-radius.sh so it is also runnable standalone.
  - name: knock-blast-radius
    files:
      - blast-radius.sh=../../scripts/blast-radius.sh
  # The DT bootstrap script, mounted into its one-shot Job. Canonical copy
  # lives at scripts/dt-bootstrap.py so it is also inspectable standalone.
  - name: knock-dt-bootstrap
    files:
      - dt-bootstrap.py=../../scripts/dt-bootstrap.py
  # The publish-sbom scripts, mounted into the publish-sbom Job. Canonical copies
  # live at scripts/ so they are also runnable standalone.
  - name: knock-publish-sbom
    files:
      - publish-sbom.sh=../../scripts/publish-sbom.sh
  # The scan-attach script, mounted into the scan-attach Job (canonical copy at scripts/).
  - name: knock-scan-attach
    files:
      - scan-attach.sh=../../scripts/scan-attach.sh
  # The XZ incident seed: the build script + the fixture Dockerfile (buildctl's context),
  # mounted together at /ctx so buildkitd builds the deliberately-vulnerable fixture in-cluster.
  - name: knock-seed-incident
    files:
      - seed-incident.sh=../../scripts/seed-incident.sh
      - debian-xz.Dockerfile=../incidents/debian-xz.Dockerfile

# Container image is rewritten per overlay (knock:dev loaded into kind for the
# demos; a pinned ghcr.io tag in prod).
images:
  - name: knock
    newName: knock
    newTag: dev
