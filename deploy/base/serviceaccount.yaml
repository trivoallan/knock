# houba needs no Kubernetes API access — it talks to registries, not the cluster.
# A dedicated SA with token automounting off keeps the blast radius of the pod minimal.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: houba
automountServiceAccountToken: false
