apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dependency-track-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi   # H2 + Lucene index + the OSV/NVD mirror; ponytail: demo size, not prod
