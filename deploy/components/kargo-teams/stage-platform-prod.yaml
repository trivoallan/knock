apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: platform-prod
  namespace: houba
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: platform
      sources:
        direct: false
        stages:
          - platform-dev
  promotionTemplate:
    spec:
      steps:
        - uses: compose-output
          config:
            output: prod
  verification:
    analysisTemplates:
      - name: houba-scan-gate
    args:
      - name: image-ref
        value: '${{ imageFrom("registry.houba.svc.cluster.local:5000/platform/debian").RepoURL + "@" + imageFrom("registry.houba.svc.cluster.local:5000/platform/debian").Digest }}'
