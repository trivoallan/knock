# houba — architecture deck

A narrative Slidev deck of the architecture (mirrors `docs/explanation/architecture.md`).

```bash
npx @slidev/cli@latest slides.md        # live dev server
npx @slidev/cli@latest build slides.md  # static build into dist/
```

Local talk artifact — not published by CI.
